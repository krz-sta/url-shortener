services:
  postgres:
    image: postgres:16
    container_name: urlshortener-db
    environment:
      - POSTGRES_DB=urlshortener
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=POSTGRES_PASSWORD
    ports:
      - "5432:5432"